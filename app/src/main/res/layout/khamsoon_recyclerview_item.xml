<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="@dimen/x_small"
    android:layout_marginTop="@dimen/small"
    android:layout_marginEnd="@dimen/x_small"
    android:elevation="@dimen/card_elevation"
    app:cardCornerRadius="@dimen/card_radius">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:id="@+id/dua_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/standard"
            android:fontFamily="@font/tajawal"
            android:gravity="center"
            android:lineSpacingExtra="@dimen/dua_line_spacing"
            android:textColor="@color/black"
            android:textSize="@dimen/dua_textsize"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="@string/tools_khamsoon_dua" />

        <TextView
            android:id="@+id/textview_references"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="16dp"
            android:fontFamily="@font/tajawal"
            android:lineSpacingExtra="@dimen/references_line_spacing"
            tools:text="@string/tools_khamsoon_references"
            android:textSize="@dimen/references_textsize"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="@+id/dua_text"
            app:layout_constraintStart_toStartOf="@+id/dua_text"
            app:layout_constraintTop_toBottomOf="@+id/dua_text" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>